# tests

純粋関数（`src/csv.js` / `src/calc.js`）のユニットテスト。`npm test`（Vitest）。

## ⚠ fixtures は個人健康データ
`tests/fixtures/*.csv` は実際のInBody測定値（測定日・体組成）を含む。
**ローカル限定**。GitHub等の外部公開リポジトリにコミットしないこと（公開する場合は値をマスキング）。
