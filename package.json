{
  "name": "kintore-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "筋トレ データダッシュボード（PWA・ノービルド）。src/ がESMモジュール、CLIは .cjs。",
  "scripts": {
    "serve": "node server.cjs",
    "gen-sw": "node gen-sw.cjs",
    "test": "vitest run --dir tests",
    "test:watch": "vitest --dir tests",
    "typecheck": "tsc --noEmit",
    "check": "node check-inbody.cjs",
    "release": "npm test && npm run typecheck && node check-inbody.cjs && node build-deploy.cjs"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
